// apps/web/wrangler.jsonc — vinext Cloudflare Workers configuration
{
  "name": "holding-web",
  "compatibility_date": "2026-01-01",
  "compatibility_flags": ["nodejs_compat"],
  "main": "./worker/index.ts",

  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "holding-dev",
      "database_id": "local-dev",
      "migrations_dir": "../../prisma/migrations"
    }
  ],

  // Dev vars — set empty so the HubSpot script is a no-op locally
  "vars": {
    "HUBSPOT_PORTAL_ID": ""
  },

  // Multi-environment configuration
  // Note: env-specific vars are patched at deploy time by scripts/cf-deploy.mjs
  // because vinext only exports the top-level wrangler config, not env overrides.
  "env": {
    "preview": {
      "name": "holding-web-preview",
      "d1_databases": [
        {
          "binding": "DB",
          "database_name": "holding-preview",
          "database_id": "61f78701-4ff5-4808-8d05-c1be66d984ec",
          "migrations_dir": "../../prisma/migrations"
        }
      ]
    },
    "staging": {
      "name": "holding-web-staging",
      // Issue PRO-165: Route staging.production.city/* to this Worker (requires DNS CNAME — see PRO-165)
      "routes": [
        { "pattern": "staging.production.city/*", "zone_name": "production.city" }
      ],
      "d1_databases": [
        {
          "binding": "DB",
          "database_name": "holding-staging",
          "database_id": "527c2f52-144b-422f-aa16-46e5b86cdd9f",
          "migrations_dir": "../../prisma/migrations"
        }
      ]
    },
    "production": {
      "name": "holding-web-production",
      // Issue #97: Route canonical domain and www (www redirects to canonical in the Worker)
      "routes": [
        { "pattern": "production.city/*", "zone_name": "production.city" },
        { "pattern": "www.production.city/*", "zone_name": "production.city" }
      ],
      "d1_databases": [
        {
          "binding": "DB",
          "database_name": "holding-production",
          "database_id": "2b77f155-690e-4368-800d-ee52e7796d81",
          "migrations_dir": "../../prisma/migrations"
        }
      ]
    }
  }
}
