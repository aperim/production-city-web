import { Meta } from "@storybook/addon-docs/blocks";
import { GuidePage } from "./GuidePage";

<Meta title="Style Guide/WCAG 2.2 AA Compliance" />

<GuidePage
  title="WCAG 2.2 AA Compliance"
  summary="This page documents the WCAG 2.2 AA compliance status of the Production City design system. axe-core runs in CI on every PR. Any criterion not fully met is documented here with a tracking issue."
  sections={[
    {
      title: "Perceivable",
      children: (
        <ul>
          <li><strong>1.1.1 Non-text Content (A):</strong> ✓ All Icon components require an aria-label or aria-hidden. Avatar requires alt text when src is provided.</li>
          <li><strong>1.3.1 Info and Relationships (A):</strong> ✓ Semantic HTML. Input, Select, Checkbox, and RadioGroup use label elements associated via htmlFor/useId.</li>
          <li><strong>1.3.2 Meaningful Sequence (A):</strong> ✓ No CSS-only reordering. DOM order matches visual order.</li>
          <li><strong>1.3.3 Sensory Characteristics (A):</strong> ✓ No component communicates state by colour alone. Error states use both colour and text.</li>
          <li><strong>1.3.4 Orientation (AA):</strong> ✓ No component restricts orientation.</li>
          <li><strong>1.3.5 Identify Input Purpose (AA):</strong> ✓ Input type prop drives the native HTML type attribute. Autocomplete attributes are passed through as HTML attributes.</li>
          <li><strong>1.4.1 Use of Colour (A):</strong> ✓ Error states use errorMessage text in addition to red border.</li>
          <li><strong>1.4.3 Contrast (Minimum) (AA):</strong> ✓ Design tokens provide 4.5:1 minimum contrast for normal text in both light and dark themes. Verified with the token palette. axe-core colour-contrast rule disabled in unit tests to avoid false positives from computed styles in jsdom (evaluated in Storybook a11y panel instead).</li>
          <li><strong>1.4.4 Resize Text (AA):</strong> ✓ All sizes use rem units. Layouts are fluid.</li>
          <li><strong>1.4.5 Images of Text (AA):</strong> ✓ No images of text used in any component.</li>
          <li><strong>1.4.10 Reflow (AA):</strong> ✓ Components are responsive. Tested at 320px viewport width.</li>
          <li><strong>1.4.11 Non-text Contrast (AA):</strong> ✓ Input borders, checkbox boxes, and radio circles use border-border token which meets 3:1 against background.</li>
          <li><strong>1.4.12 Text Spacing (AA):</strong> ✓ No component uses fixed heights that would clip text with increased line-height or letter-spacing.</li>
          <li><strong>1.4.13 Content on Hover or Focus (AA):</strong> ✓ Tooltip is dismissible (Escape key), persistent until focus moves, and does not obscure the trigger.</li>
        </ul>
      ),
    },
    {
      title: "Operable",
      children: (
        <ul>
          <li><strong>2.1.1 Keyboard (A):</strong> ✓ All interactive components (Button, Input, Select, Checkbox, RadioGroup, Toggle, Tooltip, Link) are keyboard operable.</li>
          <li><strong>2.1.2 No Keyboard Trap (A):</strong> ✓ No component traps keyboard focus.</li>
          <li><strong>2.4.3 Focus Order (A):</strong> ✓ Focus order follows DOM order in all components.</li>
          <li><strong>2.4.4 Link Purpose (A):</strong> ✓ Link component requires descriptive content. No generic "click here" enforcement at the component level — consuming app responsibility.</li>
          <li><strong>2.4.7 Focus Visible (AA):</strong> ✓ All interactive components use focus-visible:outline-2 focus-visible:outline-offset-2. No component removes focus ring without replacement.</li>
          <li><strong>2.4.11 Focus Appearance (AA, WCAG 2.2 new):</strong> ✓ Focus indicators are 2px offset outlines using the ring token. Passes the area and contrast requirements.</li>
          <li><strong>2.5.3 Label in Name (A):</strong> ✓ Button and Link text content is the accessible name.</li>
          <li><strong>2.5.7 Dragging Movements (AA, WCAG 2.2 new):</strong> ✓ No drag-only interactions in any component.</li>
          <li><strong>2.5.8 Target Size (Minimum) (AA, WCAG 2.2 new):</strong> ✓ Button sm (h-8, 32px) is below the 24px threshold. All interactive targets meet 24×24px. For kiosk use, the 64px recommendation is documented in the Audience Matrix.</li>
        </ul>
      ),
    },
    {
      title: "Understandable",
      children: (
        <ul>
          <li><strong>3.1.1 Language of Page (A):</strong> ✓ The lang attribute is a page-level concern. Design system does not set it. Consuming app responsibility.</li>
          <li><strong>3.3.1 Error Identification (A):</strong> ✓ Form controls with errorMessage render a visible error text and set aria-invalid.</li>
          <li><strong>3.3.2 Labels or Instructions (A):</strong> ✓ All form controls expose a label prop. Required fields: consuming app responsibility to add "(required)" or aria-required.</li>
          <li><strong>3.3.7 Redundant Entry (AA, WCAG 2.2 new):</strong> ✓ Not applicable — the design system does not implement multi-step forms that would require redundant entry prevention.</li>
          <li><strong>3.3.8 Accessible Authentication (Minimum) (AA, WCAG 2.2 new):</strong> ✓ Not applicable — no authentication UI in the design system.</li>
        </ul>
      ),
    },
    {
      title: "Robust",
      children: (
        <ul>
          <li><strong>4.1.2 Name, Role, Value (A):</strong> ✓ All interactive components use semantic HTML elements (button, input, select, etc.) with correct ARIA attributes. Toggle uses role="switch" with aria-checked.</li>
          <li><strong>4.1.3 Status Messages (AA):</strong> ✓ Not applicable in primitive components. Application-level status messages (toasts, alerts) must use role="status" or role="alert" in the consuming app.</li>
        </ul>
      ),
    },
    {
      title: "Automated Enforcement",
      children: (
        <ul>
          <li>axe-core runs on Button, Badge, and Text in CI via <code>pnpm --filter ./packages/ui test:a11y</code>.</li>
          <li>Storybook addon-a11y runs on all stories in the Storybook browser with <code>test: "error"</code> severity — violations fail the story.</li>
          <li>The axe-core test suite will be expanded to cover all atoms as stories are added (tracked in ongoing work).</li>
          <li>Colour contrast is excluded from the jsdom axe-core test (no computed styles available) — evaluated in the Storybook a11y panel against real renders.</li>
        </ul>
      ),
    },
  ]}
/>
